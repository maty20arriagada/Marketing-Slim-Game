# Variables de Entorno — Antigravity B2B Simulator
# Copia este archivo a .env y completa los valores antes de arrancar.
# El archivo .env NUNCA debe subirse a GitHub (está en .gitignore).

# ── Servidor ────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development    # development | production

# ── LLM / Motor IA (agregar cuando integres el motor Python) ────
# LLM_PROVIDER=google           # google | openai | anthropic
# LLM_API_KEY=tu-api-key-aqui

# ── Autenticación futura (cuando agregues login de equipos) ─────
# SESSION_SECRET=cambia-este-valor-super-secreto
