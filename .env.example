# Variables de Entorno — Marketing Slim Game
# Copia este archivo a .env y completa los valores antes de arrancar.
# El archivo .env NUNCA debe subirse a GitHub (está en .gitignore).
# En Railway estas variables se configuran en el dashboard (no se usa .env).

# ── Servidor ────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production          # development | production

# ── Seguridad ───────────────────────────────────────────────────
# Contraseña del panel del profesor (cámbiala antes de desplegar!)
PROFESSOR_PASSWORD=mktslim2025

# Secreto para firmar las cookies de sesión (genera uno aleatorio)
# Puedes generar uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=cambia-este-valor-por-uno-aleatorio-y-secreto
